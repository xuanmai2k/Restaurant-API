server:
  port: 8081
  servlet:
    context-path: /r2s-sample-api

db:
  port: 3306
  name: r2s_sample_db
  endpoint: localhost
  username: root
  password:

spring:
  datasource:
    url: jdbc:mysql://${db.endpoint}:${db.port}/${db.name}
    username: ${db.username}
    password: ${db.password}

